# ------------------------------------------------------------------
# SIGMA Studio — Web (apps/web)
# ------------------------------------------------------------------

# URL da UI do SIGMA Emu, usada na seção "Laboratório / Emulador"
# (botão "Abrir no Emulador"). Padrão: http://localhost:5174
VITE_EMU_WEB_URL=http://localhost:5174
